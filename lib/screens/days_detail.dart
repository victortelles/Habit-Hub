import 'package:flutter/material.dart';

class DaysDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Tus Dias")),
      body: Center(child: Text("Aquí aparecerán tus dias")),
    );
  }
}
