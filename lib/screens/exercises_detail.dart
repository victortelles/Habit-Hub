import 'package:flutter/material.dart';

class ExercisesDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Tus Ejercicios")),
      body: Center(child: Text("Aquí aparecerán tus Ejercicios")),
    );
  }
}
