import 'package:flutter/material.dart';

class HabitsDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Tus Hábitos")),
      body: Center(child: Text("Aquí aparecerán tus hábitos")),
    );
  }
}
